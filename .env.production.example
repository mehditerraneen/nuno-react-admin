# Production Environment Configuration Example
# This file shows the environment variables you can set in Dokploy or your deployment platform
# DO NOT commit actual credentials to version control

# Backend API URL (required)
# Replace with your actual backend URL
VITE_SIMPLE_REST_URL=http://your-backend-url:8000/fast

# Authentication credentials (required)
# Use secure credentials in production
VITE_TEST_USERNAME=admin
VITE_TEST_PASSWORD=change-this-secure-password

# JWT Token Secret (required)
# Generate a strong secret: openssl rand -base64 32
VITE_JWT_SECRET=your-strong-secret-key-here

# Environment (optional, defaults to production)
VITE_ENV=production

# Debug mode (optional, defaults to false)
VITE_DEBUG=false

# Frontend port (optional, defaults to 8080)
# Note: Port 3000 is typically used by Dokploy itself
FRONTEND_PORT=8080
